{
  "name": "premium-portfolio-root",
  "version": "1.0.0",
  "description": "Root orchestration for the Personal Portfolio Website",
  "main": "backend/server.js",
  "scripts": {
    "install:all": "npm install && cd backend && npm install",
    "start": "node backend/server.js",
    "dev": "nodemon backend/server.js"
  },
  "dependencies": {},
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
